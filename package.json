{
  "name": "shaders",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive WebGL shader experiments — dev server for previewing and live-tuning the shader studio.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.3"
  }
}
